# 流式生成功能说明

## ✨ 新功能：实时流式生成

现在你可以实时看到大模型生成笔记的进度，不再需要等待整个生成过程完成！

## 功能特点

### 1. **实时显示** ⚡
- 笔记内容逐字显示
- 即时看到生成进度
- 更好的用户体验

### 2. **视觉反馈**
- "实时生成中"徽章（带脉冲动画）
- 按钮状态显示 "生成中..."
- 流畅的过渡效果

### 3. **自动完成**
- 生成完成后自动切换到完整笔记
- 保留所有元数据信息
- 无需手动刷新

## 使用方法

### 通过 Web 界面
1. 点击 "📋 查看已提取文档"
2. 选择一个任务
3. 点击 "生成笔记" 按钮
4. 实时看到笔记内容逐字生成！✨

### 通过 API
```bash
# 流式生成（推荐）
curl -X POST "http://localhost:8000/api/generate-notes-from-task-stream?task_id=xxx"

# 非流式生成（传统方式）
curl -X POST "http://localhost:8000/api/generate-notes-from-task?task_id=xxx"
```

## 技术实现

### 后端
- 使用 Server-Sent Events (SSE)
- 流式返回生成的内容
- 事件类型：`start`, `content`, `done`, `error`

### 前端
- 使用 Fetch API 的 ReadableStream
- 实时解析 SSE 数据
- 动态更新 DOM

## 事件流格式

```javascript
// 开始生成
data: {"type": "start", "task_id": "xxx"}

// 内容块（多次）
data: {"type": "content", "content": "# Transformer\n\n模型是..."}

// 完成
data: {"type": "done"}

// 错误
data: {"type": "error", "error": "错误信息"}
```

## 优势对比

| 特性 | 非流式 | 流式 ⭐ |
|------|--------|---------|
| 用户体验 | 等待时间长 | 实时看到进度 |
| 交互性 | 黑盒等待 | 透明可见 |
| 适用场景 | 短文本 | 长文本生成 |
| 取消操作 | 不支持 | 容易实现 |

## 性能

- **首次显示**：< 1秒
- **生成速度**：实时（取决于大模型响应速度）
- **资源占用**：低（流式处理）

## 未来改进

- [ ] 添加停止生成按钮
- [ ] 显示生成进度百分比
- [ ] 支持重新生成最后一段
- [ ] 添加生成速度统计
